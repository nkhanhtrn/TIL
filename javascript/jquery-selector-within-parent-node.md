# Select element within a parent node in jQuery

jQuery provides a simple way to select an element inside a node:

```javascript
$('css-seletor', parentNode)
```

`parentNode` can be jQuery object or Node object.
